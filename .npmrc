# Force npm to run node-gyp with python 3
python=/usr/bin/python3

# Set production flag to avoid installing devDependencies
production=false

# Use legacy peer deps to avoid issues with npm 7+
legacy-peer-deps=true

# Increase network timeout for slow connections
network-timeout=100000